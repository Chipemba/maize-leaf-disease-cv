# Dataset Notes

## Primary Project Dataset

The main project uses a maize leaf field-image dataset for multi-label disease/condition classification.

The dataset is not committed to GitHub because the raw image folder is large.

Expected local structure:

```text
data/raw/
├── Database.csv
└── static/
    └── leaf_images/